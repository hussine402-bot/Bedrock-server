name: Bedrock Server

on:
  workflow_dispatch:

jobs:
  run-server:
    runs-on: ubuntu-latest

    steps:
    - name: Download & Extract Bedrock Server
      run: |
        sudo apt-get update
        sudo apt-get install -y curl unzip libcurl4 wget

        # تحميل السيرفر مع متابعة التحويلات (-L) ومتصفح كامل
        curl -L -A "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" -o bedrock-server.zip "https://minecraft.azureedge.net/bin-linux/bedrock-server-1.20.81.01.zip"

        # فك الضغط عن الملف
        unzip -o bedrock-server.zip -d server
        cd server
        chmod +x bedrock_server

    - name: Enable Cracked Mode (online-mode=false)
      run: |
        cd server
        sed -i 's/online-mode=true/online-mode=false/g' server.properties

    - name: Setup & Run Playit.gg Tunnel
      run: |
        curl -SsL https://playit-cloud.github.io/ppa/key.gpg | sudo apt-key add -
        sudo curl -SsL -o /etc/apt/sources.list.d/playit-cloud.list https://playit-cloud.github.io/ppa/playit-cloud.list
        sudo apt-get update
        sudo apt-get install playit -y
        playit &

    - name: Run Minecraft Bedrock Server
      run: |
        cd server
        LD_LIBRARY_PATH=. ./bedrock_server
